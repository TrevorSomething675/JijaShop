@using JijaShop.Api.ViewModels;
@model OwlCarouselViewModel

<div class="container">
    <div class="owl-carousel py-1">
        @foreach(var product in Model?.Products)
        {
            <div class="owl-carousel-item product-card p-1">
                <div class="col product-big-img">
                    <a asp-area="UserArea" asp-controller="Products" asp-action="Product" asp-route-productName="@product.Name">
                        <img src="@product.ProductImage.ImagePath" />
                    </a>
                </div>

                <div class="col">
                    <div class="row">

                        <div class="col-6">
                            <h2 class="text-center my-2 mx-1">@product.Name</h2>
                        </div>

                        <div class="col-6">
                            <div class="product-details-big-price d-flex justify-content-end m-1 px-1">
                                @if (product.ProductDetails.OldPrice != null && product.ProductDetails.OldPrice != 0)
                                {
                                    <small class="">@product.ProductDetails.OldPrice$</small>
                                }
                                @product.ProductDetails.Price$
                            </div>
                        </div>

                    </div>
                </div>

                <div class="col px-2">
                    <p style="overflow:hidden">
                        @product.ProductDetails.Description
                    </p>
                </div>

            </div>
        }
    </div>
</div>